import React, {useState, useEffect} from 'react';
import '../App.css';

function NotFound() {
    return(
        <h1 className="NotFound">Somewhere far far away, you might find an answer, but not here sadly</h1>
    );
}

export default NotFound;
